use crate::card::Card;
use crate::sorting::sort_cards;
use crate::can_libs::*;
use crate::can_have_straight_flush::*;

pub fn outs_strflush(hand: &Vec<Card>, comcards: &Vec<Card>) -> u16 {
   // get remainding cards
   // play out all combinations
   // use get_nut_rank's first tuple to see who 
}